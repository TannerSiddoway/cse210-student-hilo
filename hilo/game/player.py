from director
class player:

    def __init__(self):
        self.score = score
   
    def can_guess():
        return  (self.score > 0)
    
    def point_change():
        if guess == correct:
            score += 100
        else:
            score -= 75
    
    
